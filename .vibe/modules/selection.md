# Module: selection
**Role:** Renders the character roster grid and lets each of the two local players independently pick a character — including a mirror match — gating a Continue action on both picks.
**Files:** `src/selection/roster-screen.ts`
**Exports:** `renderRosterScreen(root, entries, options): void`, `RosterScreenOptions`
**Depends on:** `modules/roster.md` (consumes `DiscoveredCharacter`)
